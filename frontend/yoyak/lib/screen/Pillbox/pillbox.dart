import 'package:flutter/material.dart';

class PillBoxScreen extends StatelessWidget {
  const PillBoxScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Pill Box'),
      ),
      body: const Center(
        child: Text('Pill Box'),
      ),
    );
  }
}
